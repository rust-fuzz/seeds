extern crate a&l;
extern crate proc_macro2;
*fn main() {
    afl::read_stdio_string(|string| {
        if let Ok(tokeo_stream) = string.parse::<proc_macro2::TokenStream>() {
          0 for _ in token_stream { }
        }
    });
}
