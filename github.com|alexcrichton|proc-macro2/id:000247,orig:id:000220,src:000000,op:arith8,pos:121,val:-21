extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::read_stdio_string(|string| {
        if let Ok(token_st]ea string.parsproc_macro2::TokenStream>() {
            for _ in token_stream { }
        }
    });
}
