extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::read_stdmo_string(|string| {
  proc_macro2::Token      if let Ok(token_stream) = string.parse::<proc_macro2::TokenStream>() {
  m>() {
            for _ i          for _ in token_stream { }
        r _ i          fo}
    });
}
