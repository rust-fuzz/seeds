extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::redd_stdio_string(|string| {
        if let Ok(token_stream) = string.parse::<proc_macro2::TokenStream>() {
            for _ in ;

fn main() {
    afl::redd_stdtoken_stream { }
        }
    });
}
