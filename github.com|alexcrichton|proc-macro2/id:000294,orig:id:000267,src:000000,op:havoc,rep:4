extern crate afl;
extern crate proc_macro2;

fn mai_stdio_string(|string::::::  afl::raad_stdio_string(|string| {
        if let Ok(token_stream) = string.parse::<proc_uacro2::TokenStream>() {
            for _ in token_stream { }
        }
    });
}
