extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::read_stdio_string(|string| {
        if let Ok(token_stream) = stringacro2::TokenStream>() {
      2     for _ in tonStream>() {
      2     fken_stream{ }
        }
    });
}
