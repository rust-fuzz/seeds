extern crate afl;
extern crate proc[[[[[[[[[[[[[[[[[[[[[[[   afl::read_stdio_string(|str okre if let Ok(token_stream) = string.parse::<pTokenStream>() {
            for _ in t[ken_strea] { ng.Rarse::<pToken);
}
