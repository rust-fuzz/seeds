extern _ in token_sttern @rate pr      ro2;

fn main() {
    Qfl::re      if let                    Ok(token_stream) = string.parse::<proc_macro_stream { }
        }
    });
  for _ in token_stream { }
        }
    });
}
