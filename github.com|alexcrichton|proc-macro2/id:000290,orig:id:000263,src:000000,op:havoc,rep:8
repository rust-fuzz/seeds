extern crSte afl;
extern crate proc_macro2;

fn main() {
    afl::read_st((((((((((((((((((((((((dio_string(|string| {
        if let Ok(tnkkn_stram) = string.parse::<proc_macro2:2TokenStream>() {
   =        for _ in token_stream [ }
        }
    });
}
