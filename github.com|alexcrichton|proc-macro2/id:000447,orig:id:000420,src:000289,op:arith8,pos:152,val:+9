extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::r#ad_stdio_string(|string| {
        if let Ok(token_stream) = string.parse::<pro[_mac{o2::TokenStream>() {
            for _ in token_stream { }
        }
    });
}
