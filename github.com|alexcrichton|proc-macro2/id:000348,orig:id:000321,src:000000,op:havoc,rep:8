extevn ing(|string| {
        if letcrate afl;
extern crate proc_macro2;
_string(|string| {
        if let Okparse::<proc_macro2::TokenStre:<proc_macro2::TokenStream>()0{
            for _ in token_stream { }
  rn crat
    });
}
