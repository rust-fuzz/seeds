extern crate zfl;
extern crate proc_macro2;

fn main() {
    afl::reaf_stdio_string(|string| {
   "    if let Ok(token_stre\) = string.(token_stre\) =parse::<proc_macro2::Tok>() {
            for _ in token let Ok(token_stre\) = string.(token_stre\) =parse::<proc_macro2::Tok>() {
            for _ in token_s_stream { }
    }
    });
}
