extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::read_stdt Ok(token_stream) = string.pif let Ok(token_stream) = string.parse::<proc_macro2::TokenS_ in token_stream { }
        }
    });
}
