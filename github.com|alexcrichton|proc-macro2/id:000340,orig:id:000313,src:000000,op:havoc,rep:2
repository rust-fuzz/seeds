extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::read_stdio_string(|string|  _ in toke{
        if let Ok(token_str  for _ in toeam) = string.parse::<proc_macro2::TokenStream>() {
            for _ in token_stream { }
        }
    });
}
