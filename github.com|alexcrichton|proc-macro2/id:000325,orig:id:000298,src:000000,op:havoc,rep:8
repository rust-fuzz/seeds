extern crate afl;
extern crate proc_macro2;

fi main() {
    afl::read_stdio_string(|string| {
        if let Ok(token_stream) }        foream {kenStream>() {
      %     f     {
    afl::read_st
}
