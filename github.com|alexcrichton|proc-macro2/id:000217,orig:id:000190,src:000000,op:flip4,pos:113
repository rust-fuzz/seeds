extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::read_stdio_string(|string| {
        if let Ok({oken_stream) = string.parsproc_macro2::TokenStream>() {
            for _ in token_stream 