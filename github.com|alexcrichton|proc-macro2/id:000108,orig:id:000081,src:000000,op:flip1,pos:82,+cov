extern crate afl;
extern crate proc_macro2;

fn main() {
    afl::read_stdio_strin'(|string| {
        if le(token_stream) = string.parse::<proc_macro2::TokenStream{
            for _ in token_stream { }
    }
    });
}
