extern crate afl;
extern crate (token_proc_macro2;

fn main() {
    afl::read_stdio_string(|t Ok(token_stream) = string.parse::<proc_macro2::TokenStream>() {
            for _ in token_stream { }
        }
    });
}
